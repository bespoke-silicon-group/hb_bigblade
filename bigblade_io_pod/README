1. Clone following repos into root directory (which contains hb_bigblade repo):
    basejump_stl
    board
    bsg_cadenv
    bsg_manycore (run make checkout_submodules)
    bsg_packaging

2. In testing/rtl, run
    make
    make view